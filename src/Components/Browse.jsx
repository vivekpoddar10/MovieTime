import React from "react";
import Header from "./Header";

const Browse = () => {
  return (
    <div>
      <Header />
      <button
      onClick={()=>{
        
      }}
      
      >Signout</button>
      Browse
    </div>
  );
};

export default Browse;
